package com.example.weather_forecast_app.ui
/*
import android.view.View
import android.widget.Button
import android.widget.TextView
import androidx.recyclerview.widget.RecyclerView
import com.example.weather_forecast_app.R

/**
 * ViewHolder の実装（各要素を表示するための View を保持する）
 */
class MyViewHolder(private val view: View) : RecyclerView.ViewHolder(view) {
    val info1: TextView by lazy {
        view.findViewById<TextView>(R.id.info1)
    }

    val info2: TextView by lazy {
        view.findViewById<TextView>(R.id.info2)
    }

    val info3: TextView by lazy {
        view.findViewById<TextView>(R.id.info3)
    }

    val info4: TextView by lazy {
        view.findViewById<TextView>(R.id.info4)
    }
}*/